package pt.isec.g45.tp.ui.text;

import pt.isec.g45.tp.model.AppManager;

public class AppGUI {
    AppManager app;

    public AppGUI(AppManager manager) {
        this.app = manager;
    }

    /*
    private boolean finish = false;

    public void start() {
        while (!finish) {
            switch (app.getState()) {
                case BEGIN_MENU -> begin_menu();
            }
        }
    }
    */
}
